{
  "name": "police-cad-suite",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "npm run dev -w server & npm run dev -w client",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  }
}
